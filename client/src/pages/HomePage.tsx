import { Layout } from "antd";
import {HomePageContent} from "../features/HomePageContent/HomePageContent.tsx";

export const HomePage = () => {
    return (
        <Layout >
            <HomePageContent/>
        </Layout>
    )
}

export default HomePage
